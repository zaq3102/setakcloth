import * as React from 'react';
import '../styles/User.scss';
import Slider from './common/Slide';

const Home: React.FC = () => {
  return (
    <div className="wrap">
      <Slider />
    </div>
  );
};

export default Home;
